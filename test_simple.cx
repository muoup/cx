#include <stdio.h>

struct MyStruct {
    int x;
    int y;
};

// Static member function without 'this' parameter
MyStruct MyStruct::create(int x, int y) {
    return { .x = x, .y = y };
}

int main() {
    // Try just the access without the call
    MyStruct::create;
    return 0;
}