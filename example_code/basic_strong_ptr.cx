import std::memory;

struct strong_ptr_container {
    int other_data;
    int strong* ptr;
};

int main() {
    int strong* ptr = __stdalloc(4);
    int strong[] ptr2 = __stdarrayalloc(4, 2);

    strong_ptr_container container;
    container.ptr = __stdalloc(4);

    strong_ptr_container strong* container_ptr = __stdalloc(sizeof(strong_ptr_container));
    strong_ptr_container strong[] container_array = __stdarrayalloc(sizeof(strong_ptr_container), 2);
}