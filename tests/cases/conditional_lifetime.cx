import std::io;

struct Lifetime {};

~Lifetime(this) {
    println("Lifetime destroyed");
}

int main() {
    int cond = 1;

    if (cond) {
        int x = 10;
    } else {
        Lifetime lifetime;
    }

    if (cond) {
        Lifetime lifetime;
    } else {
        int y = 20;
    }
}