import std::vector;
import std::io;

int main() {
    vector<int> vec = create_vector<int>();

    vec.push<int>(1);
    vec.push<int>(2);
    vec.push<int>(3);

    printf("Vector length: %zu\n", vec.length);
    printf("Vector capacity: %zu\n", vec.capacity);
    printf("vec[0]: %d\n", *(vec.at<int>(0)));
}