#include <stdio.h>

enum Test {
    a,
    b = 2,
    c
 };

int main() {
    printf("a: %d\n", a); // Output: a: 0
    printf("b: %d\n", b); // Output: b: 2
    printf("c: %d\n", c); // Output: c: 3
}