#include <stdio.h>

template <T : type>
struct templated_object {
    T value;
};

template <T : type>
~templated_object<T>(this) {
    printf("~templated_object\n");
}

int main() {
    templated_object<int> object;
}