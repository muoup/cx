import std::string;
import std::optional;
import std::io;

int main() {
    string s = string::new();
    s.append("Hello, worl");
    s.append_char('d');
    s.append_char('!');
    printf("%s\n", s.data);
    s.drop();
}