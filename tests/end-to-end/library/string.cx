import std::string;
import std::optional;

#include <stdio.h>

int main() {
    std::string::string s = std::string::string::new();
    s.append("Hello, worl");
    s.append_char('d');
    s.append_char('!');
    printf("%s\n", s.data);
    s.drop();
}
