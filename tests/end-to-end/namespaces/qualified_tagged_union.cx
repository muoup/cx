import std::optional;

#include <stdio.h>

int main() {
    std::optional::opt<int> value = std::optional::opt::some<int>(7);
    if (value is std::optional::opt<int>::some(inner)) {
        printf("%d\n", inner);
    }
    return 0;
}
