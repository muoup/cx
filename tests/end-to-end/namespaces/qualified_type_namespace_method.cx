import std::file;
import std::optional;

#include <stdio.h>

int main() {
    std::optional::opt<std::file::file> maybe_file = std::file::file::open("end-to-end/library/file_read_to_string.txt", "r");

    match (move maybe_file) {
        std::optional::opt::some<std::file::file>(f) => {
            printf("opened\n");
            f.close();
        }
        std::optional::opt::none<std::file::file>() => printf("missing\n");
    }

    return 0;
}
