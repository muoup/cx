import std::vector as v;

#include <stdio.h>

int main() {
    v::vector<int> vec = v::vector::new<int>();
    vec.push<int>(42);
    printf("%zu\n", vec.length);
    vec.drop<int>();
    return 0;
}
