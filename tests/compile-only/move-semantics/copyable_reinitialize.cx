struct Data {
    int value;
};

void consume(Data data) {
    return;
}

int main() {
    Data data = (Data) {
        .value = 1
    };

    consume(move data);

    data = (Data) {
        .value = 2
    };

    return data.value;
}
