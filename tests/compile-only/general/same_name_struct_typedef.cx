typedef struct FilePathList {
    unsigned int capacity;
    unsigned int count;
    char **paths;
} FilePathList;

FilePathList load_files(void);
void unload_files(FilePathList files);

int main() {
    FilePathList files = { 0, 0, (char**)0 };
    return files.count;
}
