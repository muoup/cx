int use_raw(double scale, void* raw) {
    int* typed = raw;
    double total = *typed + scale;

    if (total && raw) {
        return *typed + 1;
    }

    return 0;
}

int main() {
    int value = 3;
    void* raw = &value;
    double scale = 1.5;
    return use_raw(scale, raw) - 4;
}
