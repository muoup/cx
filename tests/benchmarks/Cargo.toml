[package]
name = "cx-benchmarks"
version = "0.0.0"
edition = "2021"

[features]
default = ["cx-test-support/default"]
backend-llvm = ["cx-test-support/backend-llvm"]

[dependencies]
cx-test-support = { path = "../support", default-features = false }
serde = { version = "1", features = ["derive"] }
serde_json = "1"
