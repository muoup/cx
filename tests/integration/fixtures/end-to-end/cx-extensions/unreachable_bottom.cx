/* CX-STDOUT: */

unreachable todo() {
    while (1) {}
}

int choose(int condition) {
    return condition ? 1 : todo();
}

int main() {
    if (0) {
        int value = todo();
        return value;
    }
    return choose(1) - 1;
}
