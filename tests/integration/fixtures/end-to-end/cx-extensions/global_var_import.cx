/* CX-STDOUT: x: 15, y: 25 */

import includes::global_variables;
import std::io as std;

int main() {
    int* y_ptr = &includes::global_variables::y;

    std::printf("x: %d, y: %d\n", includes::global_variables::x, *y_ptr);
}
