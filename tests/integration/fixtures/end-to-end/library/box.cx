/* CX-STDOUT: i = 2 */

import std::box;

#include <stdio.h>
#include <stdlib.h>

int main() {
  std::box::box<int> i = std::box::box::new<int>(2);
  printf("i = %d\n", i |> std::box::box::deref_mut<int>());
  move i |> std::box::box::drop<int>();
}
