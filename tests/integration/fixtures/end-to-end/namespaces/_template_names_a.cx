public:

T value<T>(T input) {
    return input + 1;
}

comptime expr T staged<T>(expr T input) {
    return emit input;
}

struct Box<T> {
    T value;
};

T Box::read<T>(Box<T>& this) {
    return this.value;
}
