enum union Choice {
    zero :: int,
    one :: int
};

void inspect(Choice value) {
    match (move value) {
        Choice::zero(inner) => {}
        Choice::zero(other) => {}
        _ => {}
    }
}

int main() {
    return 0;
}
