import std::vector;

int main() {
    std::vector::vector<int> ints = std::vector::vector::new<int>();
    std::vector::vector<f64> floats = move ints;

    move floats |> std::vector::vector::drop<f64>;
    return 0;
}
