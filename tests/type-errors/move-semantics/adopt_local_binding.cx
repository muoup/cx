struct Resource : @nodrop {
    int data;
};

int main() {
    Resource value = (Resource) {
        .data = 1
    };

    Resource adopted = @unsafe(@adopt(value));
    move adopted |> Resource::drop;
    return 0;
}

void Resource::drop(Resource this) {
    @leak(this);
}
