struct Resource : @nodrop {
    int data;
};

struct Container : @nodrop {
    Resource resource;
    int count;
};

void Container::drop(Container this) {
    @unpack(move this) {
        count: count,
    };
}
