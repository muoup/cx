struct Item : @nocopy {
    int data;
};

struct Container {
    Item inner;
};

void Item::consume(Item this) {}

int main() {
    Container container = (Container) {
        .inner = (Item) {
            .data = 1
        }
    };

    move container.inner |> Item::consume;
    return 0;
}
