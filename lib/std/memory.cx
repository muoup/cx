public:

void strong* stdalloc(u64 size) {
    return (void strong*) malloc(size);
}

void stdfree(void strong* ptr) {
    printf("[stdfree] memory freed!\n");
    free(move ptr);
}

private:

#include <stdio.h>
#include <stdlib.h>