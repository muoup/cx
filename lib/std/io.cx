extern "C":

#include <stdio.h>

public:

void print(const _str& s) safe {
    @unsafe { printf(s); };
}

void println(const _str& s) safe {
    @unsafe { puts(s); };
}

void print_char(i32 c) safe {
    @unsafe { putchar(c); };
}
