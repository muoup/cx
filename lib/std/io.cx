#include <stdio.h>

public:

void println(string str) {
    puts(str.c_str());
}

void print(string str) {
    printf("%s", str.c_str());
}

void printc(char c) {
    putchar(c);
}

private:

void printf(char* format, char* arg);