#include <stdio.h>

struct MyStruct {
    int x;
    int y;
};

// Static member function without 'this' parameter
MyStruct MyStruct::create(int x, int y) {
    return { .x = x, .y = y };
}

int main() {
    // Try the function call
    MyStruct s = MyStruct::create(5, 7);
    return 0;
}