[package]
name = "cx-backend-llvm"
version = "0.0.0"
edition = "2024"
autotests = false

[dependencies]
cx-data-mir = { path = "../cx-data-mir" }
cx-data-pipeline = { path = "../cx-data-pipeline" }
cx-util = { path = "../cx-util" }

inkwell = { version="0.6.0", default-features = false, features=["llvm18-1"] }